use std::str::FromStr;
use ast::Operator;

//
//    number   : /-?[0-9]+/ ;
//    operator : '+' | '-' | '*' | '/' ;
//    expr     : <number> | '(' <operator> <expr>+ ')' ;
//    lispy    : /^/ <operator> <expr>+ /$/ ;
//

grammar;

pub Operator: Operator = {
  "+" => Operator::Plus,
  "-" => Operator::Minus,
  "*" => Operator::Multiply,
  "/" => Operator::Divide,
};

